var buildWalls = {

    run: function(room) {

      var wallW = room.find(FIND_EXIT_BOTTOM;
      for (let i=0;i<wallW.length-1;i++) {
        if (wallW[i].x!=wallW[i+1].x-1) console.log('new seg');
      }
    }
};
module.exports = buildWalls;
